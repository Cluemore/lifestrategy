# Same-origin API path used by Vite while developing locally.
VITE_API_BASE_URL=/api
# The Vite development proxy sends /api requests to the local FastAPI service.
VITE_LOCAL_API_PROXY_TARGET=http://127.0.0.1:8000
