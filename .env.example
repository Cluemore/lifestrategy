# Public FastAPI base URL embedded into a Vercel production build.
# Do not include a trailing slash. Local `npm run dev` uses .env.development.
VITE_API_BASE_URL=https://your-railway-service.up.railway.app/api
