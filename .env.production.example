# Set this in Vercel for both Preview and Production before building the site.
# Vite exposes only variables prefixed with VITE_ to browser code.
VITE_API_BASE_URL=https://your-railway-service.up.railway.app/api
